stages:
    - build
    - test

package_job:
    stage: build
    image: maven:latest
    script: 
        - mvn -f user-service/pom.xml clean package
    tags:
        - Docker
    artifacts:
        paths:
            - target/    
    rules:
        - when: manual    

test_job:
    stage: test
    script:
        - echo "Testing...."
        - ls target/*
    tags:
        - Docker    
    rules:
        - when: manual    